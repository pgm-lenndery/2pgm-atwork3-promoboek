import React from 'react';
import { Container } from '..';

export default () => {
    return (
        <Container fluid>
            <h2 className="section__title">cases & opdrachten</h2>
        </Container>
    )
}